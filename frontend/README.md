# Samwaad Frontend (scaffold)

Run locally:

```powershell
cd d:\samwaad-app\frontend
pnpm install
pnpm run dev
```

The dev server runs on port 3001 by default and proxies `/api` to `http://localhost:3000`.
